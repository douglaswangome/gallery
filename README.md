# Build 1
- Failed, I saved the file as Jenkins instead of Jenkinsfile

# Build 2
- Failed, I saved the file as Jenkins instead of Jenkinsfile

# Build 3
- I have changed the file name to Jenkinsfile
- The build failed

# Build 4
- No changes made
- The build was unsuccessful
- Installed the Github plugin

# Build 5
- No changes made
- The build was unsuccessful
- Installed the Node JS tool plugin

# Build 6
- Changes made to the Jenkinsfile
- The build was unsuccessful